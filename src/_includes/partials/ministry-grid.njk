<section class="section">
    <div class="container">
        <div class="columns mt-5 is-8 is-variable is-centered">
                {% for ministry in collections.ministry %}
                    {% include "partials/ministry-card.njk" %}
                {% endfor %}
        </div>
    </div>
</section>